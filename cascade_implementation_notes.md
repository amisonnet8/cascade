# AMIVM上で言語を実装するときのヒント(Cascadeの実装から)

> 取り込み元: Cascade(`github.com/amisonnet8/cascade`)の実装過程(15ステップ+AMIVM改修6回への追従。うち2回——ジェネリクス対応+`METHVAL`/`FUNCVAL`/`FUNCM`/`INTYPE`/`GETYPE`追加、および`ARTYPE`+`imm`追加——は完全後方互換でコード変更が一切不要だった)で得られた知見。
> 次にAMIVM上で別言語を実装するAI向けの申し送り。Seed(`github.com/amisonnet8/seed`)の実装知見の続編という位置づけだが、**各言語の実装知見はその言語自身のリポジトリで管理する方針のため、Seed側の知見(goto/VAR巻き上げ・スコープのフラット化・CALLの多義性・参照渡し/値渡しなど)はこのファイルに転記しない**——必要なら`github.com/amisonnet8/seed`リポジトリ自身の実装知見を参照すること。このファイル自体はCascadeリポジトリの一部として管理する(`amivm/`のような外部クローン配下には置かない——外部クローンは更新のたびに丸ごとclone/削除され、untrackedなファイルはその過程で消える。実際にこのファイルが以前この方法で2度消えたことがあり、それが「各言語は自分のリポジトリで知見を管理する」という方針へ切り替えた直接のきっかけになった)。**Cascade自身の言語仕様の話ではなく、「AMIVM-IRを生成するフロントエンドを書くときに踏む地雷・使える型」の話に絞っている。**

## 0. Cascadeが実証した命令 — 実装当時の全64命令、未実証ゼロ(その後追加された命令は対象外)

Cascadeの言語仕様(ポインタ・構造体・クロージャー・map・チャネル/goroutine・ビット演算・エラー処理・並行パイプライン・パッケージ)は、実装当時の`amivm_spec.md`4節が定義する命令を**1つ残らず**使う結果になった。「無理に全命令を使う」のではなく、機能実装を素直に進めた結果として全命令が自然に使われた(この点は本書の各節で個別に補足する)。

その後amivm本体に追加された`ASSERT`(型アサーション)、`METHVAL`/`FUNCVAL`(メソッド値・関数値の`:=`取得)・`FUNCM`/`ENDFUNCM`(Goネイティブのレシーバー付きメソッド)・`INTYPE`/`METHOD`/`ENDINTYPE`(インターフェース)・`GETYPE`(ジェネリクス型の実体化)・`ARTYPE`(名前付き固定長配列型)は、いずれもCascadeの型システムに対応物が無い(`any`相当の型・多態・ジェネリクス・固定長配列のいずれも存在しない)ため未使用のまま——`FUNCM`だけは対応する言語機能(レシーバー付き関数)自体は存在するが、既存の「レシーバーを第一引数とする普通のFUNC」という実装で機能的に不足が無く、切り替える実益が今のところ無い(判断根拠はCascadeリポジトリの`CLAUDE.md`「命令使用ゴール」表を参照)。

## 1. ポインタ(`ADDR` `PGET` `PSET`) — Seedの推測は半分だけ当たっていた

- **`ADDR`/`PGET`/`PSET`/`FSET`は、いずれも「変数」オペランドが裸の`$N`/`&N`/`%xxx`/`@xxx`のみ**で、構造体フィールドパス(`p.x`)やリスト添字(`xs[0]`)を直接の対象にはできない、という制約が当初あった。
- **重要な非対称性**: 「読み取り」(`PGET`)と「複合的な書き込み」(`FSET`/`PSET`/`ASET`の連鎖)は、**コピー往復で常に回避できる**(`FGET`で一時変数へ読み出す→書き換える→`FSET`で書き戻す。1つの文の中で完結する単発の往復だから)。しかし**`&`(アドレス取得)だけはコピー往復で絶対に回避できない**——アドレス取得は「元のストレージへの持続的なエイリアス」を作る操作であり、コピーをADDRしてもコピー自身を指すだけで元のストレージとは二度とつながらない。
- Cascadeは最初、この制約をそのまま受けて`&`を裸の識別子のみに制限した。後日amivm本体に`ADDR`の`point`オペランド(単一階層のフィールド名`>xxx`、または添字)が追加され、`&variable.point`/`&variable[point]`という形が使えるようになった。ただし**`point`は経路(パス)ではなく単一階層のみ**(`&a.b.c`のような多段は非対応)、かつ**mapの添字には使えない**(Go自身がmap要素をアドレス不可とすることに合わせた仕様)。
- **codegen側の落とし穴**: `&p.x`を実装する際、「対象式を`genValue`で一度値として評価してからADDRする」という他の演算子で普段使う定石をそのまま適用すると、`FGET`/`AGET`で読み出した**コピー**をADDRしてしまい、元のフィールド/要素とは無関係な独立したポインタになる。正しくは、ベースとなる変数のトークンをスコープから直接引き、`ADDR tmp base >field`のように**ADDR一発で**フィールド/添字を折り込む必要がある。
- **この落とし穴は1箇所では済まなかった**: 値レシーバー/ポインタレシーバーの自動変換(値の構造体をポインタレシーバーメソッドへ渡す際の暗黙ADDR)も同じロジックの使い回しだったため、`&`の制約を広げた際に**同じバグが別の場所で再発しかけた**。「アドレス取得可能な式の集合」を広げるときは、その集合を参照している全箇所を横断的に洗い出すこと。
- ポインタのnull許容性は、他の`T?`型が使う「値+issetフラグ」パターンを流用せず、**Goネイティブの`nil`で表現**した。「常にnullな型はGoのゼロ値をそのまま使い、issetフラグを持たせない」という判断は、クロージャー(関数型)にも同様に適用できた(§4参照)。

## 2. 構造体(`STTYPE` `FIELD` `ENDSTTYPE` `FSET` `FGET`)

- **`FGET`/`FSET`はGoの`x.field`構文の性質(値でもポインタでも透過的にフィールドアクセスできる)をそのまま引き継ぐ**ため、値レシーバー/ポインタレシーバーどちらのメソッドでも「フィールド読み書きそのもの」にコード生成側の特別な分岐は一切要らなかった。特別扱いが必要になるのは「渡されたレシーバーの値/ポインタの種類が、メソッドが宣言する種類と食い違っている場合のブリッジ(暗黙ADDR/PGET)」だけ。
- **構造体の「宣言のみ・初期化式なし」は再帰的なゼロ初期化が必要**で、AMIVM-IRに「ゼロ構造体リテラル」に相当する直接的なトークンは無い。ネストした構造体フィールドは「フィールドを一旦フレッシュな一時変数へ`FGET`→再帰的にゼロリセット→`FSET`で書き戻す」という手順で組める(Goの構造体が値型であることを利用)。

## 3. map(`MPTYPE` `MPMAKE` `MSET` `MGET` `MPKEYS`)

- `MGET`のcomma-ok形が値+nullフラグへそのまま流し込める、という予想は**的中**した。`let v: int? = m[k]`は`MGET`の2オペランド結果を直接、値+issetフラグへ落とし込むだけで実現できた。
- **想定外の罠**: 「宣言のみ・初期化式なし」の非nullable mapは、単純に`SET nil`で済ませてはいけない。Goのnil mapは**読み取りは安全だが書き込みはpanicする**——nilスライスへの`append`が安全なのとは非対称。非nullable・初期化式なしの場合は`MPMAKE`で実体のある空mapを確保する必要がある。
- **最大の想定外**: AMIVM-IRには**mapを走査する命令が最初は存在しなかった**。これは「コピー往復で回避できる」レベルの制約ではなく、**専用のGoランタイムパッケージを新設して`slices`/`maps`相当の処理を肩代わりさせる**しか回避策が無かった。後日amivm本体に`MPKEYS single1 single2`(`single1 = slices.Collect(maps.Keys(single2))`)が追加され、このランタイム依存は完全に不要になった。**「map走査ができない」は、コピー往復で回避可能な他のオペランド制約とは重みが違う——専用ランタイムが要るレベルの制約はamivm本体への機能要求として真っ先に候補に挙げる価値がある**、という判断基準を得た(§8参照)。
- map値型(`V`)は独立してnullableにはできない(`map<K, V?>`非対応)——`m[k]`が返す`V?`自体が既に1段のnullabilityを表現しているため、`MGET`のcomma-ok形では二重のnullabilityを素直に表現できない。

## 4. クロージャー・高階関数(`FNTYPE` `CLOS` `ENDCLOS`)

- **最も価値のある発見**: 外側スコープの変数を捕捉する専用機構は一切実装せずに済んだ。`CLOS`本体をコンパイルするコード生成側の「1関数分の状態」のうち、**スコープだけをクロージャーリテラル出現時点の外側スコープの子スコープにする**だけで、クロージャー本体が外側の変数を参照すると`scope.lookup`が外側まで辿って**同じトークン**を返す。コピーも専用のセル/ボックス機構も不要。
- **`CALL`の`callname`オペランドに`$N`/`&N`(関数引数・クロージャー引数)を直接使えない制約**が当初あった。「関数の引数として受け取ったクロージャーをそのまま呼ぶ」ケースが設計段階の資料調査だけでは見つからず、**実際にamivmへIRを通して初めて発覚した**パースエラーだった。回避策は「呼び出し対象のトークンが`%`/`@`で始まっていなければ、まず新しい一時変数へ`SET`でコピーしてから呼び出す」というヘルパーへの一元化。後日amivm本体の`callname`が`$N`/`&N`/`@xxx`を直接受け付けるよう拡張され、このコピー回避ロジックは完全に不要(到達不能な防御コードとしてのみ残す)になった。
- **`CLOS`の左辺(代入先)が当初`%xxx`のみに制限されていたため、`let f = func(...) {...}`のような単純な代入でも「新規一時変数へCLOS→SETでコピー」という無駄な1段が必ず入っていた。** 後日、`CLOS`専用の新カテゴリ`shallow`(`$N`/`%xxx`/`@xxx`)が追加され、この無駄が解消された——当時`&N`(クロージャー引数)を`shallow`が含まないのは筋が通っていた: `CLOS`はネスト不可だったため、`CLOS`本体の中から`CLOS`を発行する状況が構造的に存在せず、`&N`が左辺として出現するケースは原理的にゼロだった。
- **`CLOS`のネスト対応(クロージャーを返すクロージャー、カリー化)。** amivmは当初`STTYPE`と同様に`CLOS`もネスト禁止だったが、後日この制約だけが緩和され、`CLOS`本体の中にさらに`CLOS`を書けるようになった。ネストを許すと、直前の「`shallow`が`&N`を除外する根拠」が消える——内側の`CLOS`から見れば、外側の`CLOS`のクロージャー引数(`&N`)も「既に存在する、代入し直せる変数」という点で`$N`と全く同じ扱いをして構わない。そのためamivm側は`shallow`という専用カテゴリを廃止し、`CLOS`の代入先には元々`&N`を含んでいた`single1`をそのまま流用することにした。
  - **ネストに伴い、クロージャー引数の表記が「常に固定名」から「階層付き」へ変わった。** ネスト前は`CLOS`が常に1つしか存在しなかったため、パラメータを関数名修飾なしの固定名にしても衝突しなかったが、ネストすると内側の`CLOS`が外側と同じパラメータ番号を使った場合、Goの通常のスコープルールで外側がシャドーイングされ、外側のパラメータを内側から参照する手段が失われる。解決策は`&N`を「自分がいる`CLOS`階層のN番目」、`&L-N`を「階層`L`のN番目を明示指定」と再定義すること。
  - **フロントエンド実装上の要点**: クロージャーパラメータの値を保持する変数参照(`varRef.ValOp`)は宣言時に一度だけ決まる固定文字列で、以降はどの深さの子クロージャーから参照されようと同じ文字列がそのまま再利用される。**ここでbareな`&N`省略形を宣言時に格納すると、そのクロージャー自身からの参照では正しく解決できても、さらに内側のクロージャーがそれを捕捉して参照する際には「自分自身のN番目」と誤読される。** 完全修飾形`&L-N`はどの深さから参照されても常に一意に解決できるため、ネストに対応する場合は「宣言時から常に完全修飾形で保存する」以外の安全な選択肢が無い。
- `filter`のような「出力件数が事前に分からない、入力を1回だけ走査したい、述語(predicate)が副作用を持ちうる」組み込み関数は、**「件数を数える2パス」ではなく「最悪ケースのサイズでSLMAKE→1パスで詰める→SLICEで切り詰める」**という設計にすること。2パス方式は述語を要素ごとに2回評価してしまい、副作用がある場合に壊れる。
- **フロントエンド言語側の落とし穴(amivmではなく言語設計側)**: クロージャーのネストが可能になっても、「呼び出し式の結果へ直接もう一段`(...)`を続ける」(`curry(a)(b)`のような連続呼び出し構文)は別問題として残りうる。Cascadeの構文解析は「呼び出し対象は常に名前」という前提で組まれており、呼び出し式の結果を新たな呼び出し対象にする一般化は未対応のまま残した(`let f = curry(a); f(b)`のように一段ずつ`let`で束縛すれば動く)。amivm側のCLOSネスト対応そのものとは独立した、フロントエンド自身の式文法の設計課題であることに注意。

## 5. チャネル・goroutine・select(`CHTYPE` `CHMAKE` `CHSEND` `CHRECV` `SPAWN` `DEFER` `SEL` `CASESEND` `CASERECV` `DEFAULT` `ENDSEL`)

- `CHRECV`のcomma-ok形も`MGET`と全く同じパターンで、値+issetフラグへ直結できる。
- **`DEFER`の綺麗な使い道**: パイプラインの各段(source/stage)の関数本体先頭で無条件に`DEFER ?close <出力チャネル>`を発行するだけで、「その段がどの経路で終了しても、自分の出力チャネルを自動closeして下流の`for-in`を終了させる」という後始末が、ユーザー向け構文に一切現れない形で保証できる。
- **`SPAWN`と普通の`CALL`の非対称性だけで、並行パイプライン全体の同期が完結した**: パイプラインの先頭と中間の各段は`SPAWN`、**最後の段だけ**普通の`CALL`にするだけで、明示的なjoin/WaitGroupが一切無くても「パイプライン全体の文は、最後の段が終わるまで自然にブロックする」という挙動が手に入る。
- **selectで複数goroutineから同時に「中断」をブロードキャストする際の罠**: 素朴に「共有の`chan<bool>`を1回closeするだけ」という設計は、**複数のgoroutineが同時にcloseしようとすると`close of closed channel`でパニックする**。解決パターンは「closeする権利を1つだけ配る」ための**別のバッファ付きガードチャネル(容量1)**を用意し、各呼び出し元がそこへ`SEL`+`CASESEND`+`DEFAULT`で非ブロッキング送信を試み、**送信に成功した1つだけが実際のブロードキャストチャネルをclose**する、という二重チャネル構成。
- **受信側だけでなく送信側(`CHSEND`)にも中断監視が要る**ことを見落としかけた——非バッファチャネルへの`send()`は、下流の段が先に終了して受信をやめてしまうと永遠にブロックしたまま取り残される(デッドロック)。
- 2本のチャネルをファンインするには、AMIVM-IRに「`SEL`の節を動的に増減する」手段が無いため、**「片方が閉じたら、そのチャネル変数自体をGoの`nil`にセットして、以降そのSEL節を事実上無効化する」という定番のnilチャネルイディオム**が唯一の自然な実装方法だった。

## 6. ビット演算・シフト(`BAND` `BOR` `BXOR` `BCLEAR` `BNOT` `SHL` `SHR`)

- コード生成そのものはSeedの予想通り、`ADD`/`SUB`と全く同じ「演算子ごとに命令を振り分け、一時変数へ結果を格納する」パターンがそのまま使えた。驚きは無し。
- **驚きは命令ではなく言語仕様側(演算子優先順位)にあった**: C/Go系の直感では「シフトは加減算より高優先度」だが、対象言語の仕様がその逆を選ぶ可能性は普通にある。この手の優先順位の逆転は、**amivmやgo buildのエラーとして絶対に表面化しない**(どちらの解釈でも構文的に正しいGoコードが生成され、ただ計算結果が違うだけ)。優先順位表を実装したら、具体的な数値例で明示的な回帰テストを固定しておかないと、将来のリファクタで静かに壊れても誰も気づけない。

## 7. 制御フロー命令(`IF`/`LOOP`/`SEL`)の破壊的変更と移行

amivmの単一行`IF boolean1 label`(条件付きgoto方式)は、実装当初は安定した基盤に見えたが、後日ブロック形`IF`/`ELIF`/`ELSE`/`ENDIF`と新設`LOOP`/`BREAK`/`CONTINUE`/`ENDLOOP`へ**後方互換なしで**全面置き換えされた。同時に`SEL`の`CASESEND`/`CASERECV`/`DEFAULT`も`label`を取らずブロック本体を持つ形に変わり、`IF`/`LOOP`/`CLOS`/`SEL`が全てネスト可能に統一され、`any`型アサーション用の`ASSERT`が新設された。

- **この種の「基盤となる制御フロー命令の後方互換のない全面刷新」は、amivm側が「実際に複数の言語実装を書いてみて得られた知見をIRにフィードバックする」という設計ポリシーを取っている以上、今後も起こりうる。** フロントエンド言語側は、制御フロー命令をコード生成のごく少数の箇所(if/while/for/switch文の生成関数)に閉じ込めておくと、こうした刷新への追従コストを最小化できる。Cascadeでは`internal/codegen`の`genIfStmt`/`genWhileStmt`/`genSwitchStmt`/`genForInStmt`(list/map/channel)/`genBreakStmt`/`genContinueStmt`という限られた関数群に制御フローの生成が集約されていたため、この規模の刷新でも影響範囲を正確に把握し、言語仕様・AST・意味検査には一切手を入れずに完了できた。
- ELIFトークンを直接使わず、ELSEの中に次のIFをネストして自前に構築する、という判断が有効だった。条件式が複数命令を要する場合(`a + b < c`のような)、ELIFトークン自体のオペランドは「その行に到達した時点で既に値になっている」ことを要求するため、複数命令を挟む余地がない。ネスト構造にすれば各節の条件計算をそれを守るELSEの中に自然に置け、短絡評価も無料で手に入る(1つのcaseに複数値がある`switch`のcase値評価にも同じ理由・同じ構造を適用でき、値ごとの短絡評価を保てた)。
- **switch文のような「対応するネイティブ命令がAMIVMに存在しない構文」は、IF/ELSEチェーンへdesugarするしかない。** desugarした結果、Goの`break`/`continue`キーワードの挙動と対象言語が要求する意味論(breakはswitchを抜けるがcontinueはswitchを素通りしてループへ)がズレる場合がある——Goの`break`は最も内側の`for`/`switch`/`select`を抜けるが、IF/ELSEへdesugarされたswitchはGoの本物のswitchではないため、ネイティブの`break`では正しく抜けられない。一方Goの`continue`は最も内側の`for`にしか効かず、間に挟まる`if`は自動的に素通りするため、desugarされたswitchであっても`continue`は素のGoの`continue`だけで正しく動く。**この非対称性(breakは特別扱いが要るがcontinueは要らない)が、対応するネイティブ命令を持たない構文をdesugarする際に一般的に起こりうるパターンとして記録しておく価値がある。**「制御フロー命令が全面刷新されても、LABEL/GOTOは構造化制御フローで表現できないジャンプのために残されている」というamivm側の設計判断のおかげで、この局所的な使用が可能だった。
- ブロック形IF/LOOPが本物のGoブロックスコープを生成するようになると、「gotoが変数宣言を飛び越えてはいけない」という制約はほとんどの制御構文で心配しなくてよくなる。ただし構造化制御フローで表現できない残りのLABEL/GOTO用途(switchのbreak、添字カウント式for-inのcontinue)ではこの制約が今でも生きている可能性があるため、「VAR巻き上げを全面的に撤去してよい」とまでは言い切れない。巻き上げを維持するコストは低く撤去の利益は薄いので、Cascadeでは維持したまま移行を完了させた——**理論上不要になった安全策でも、実害が薄いなら急いで剥がす必要はない。**
- **新たに踏んだバグパターン**: 「あるブロック構造(switch相当)の終端ラベルが、そのブロックの中身が特定の構文(break)を一度も使わない場合に、定義されているのに参照されない状態になる」というのは、for-inのcontinueラベル(本体にcontinueが無いと未参照になる)と全く同じ形のバグだが、**別の言語機能で独立に踏んだ**。1箇所で見つけたバグパターンを他の類似箇所に横展開して確認することを怠ると、同じ形のバグを何度も個別に発見するはめになる。対策として、「LABEL/GOTOを新設したら、そのラベルが無条件に少なくとも1回は参照されるパスを必ず用意する」という習慣を持つとよい。**この教訓はテストコード自体にも埋め込んでおく価値がある**——「定義されたラベルは必ず参照される」という不変条件を汎用ヘルパーとしてテストスイートに常設しておけば、新しい制御構文を追加するたびに同種のバグを自動検出できる。
- ブロック形SELへの移行は、単なる構文の書き味の変化ではなく、実際に生成コードを大幅に単純化する効果があった。特に「2つの独立した状態を持つ合成関数」(2チャネルのファンイン)は、旧来のlabel+goto方式では複数のラベルを要していたのが、ブロック形移行後はLABEL/GOTOを1つも使わない、SELのcase本体に直接ロジックを書くだけの形に整理できた。**新しい命令セットへ移行する際は、単に「動く」形に置き換えるだけでなく、その命令セットで最も自然な形に書き直すことで、コードの複雑さそのものを削減できないか問い直す価値がある。**

## 8. sema/codegenの責任分担で学んだこと

- amivmは型・スコープの意味検証を一切行わず、**全て`go/types`に委ねている**。つまりフロントエンド言語自身のsemaが、ユーザーのミスとgo/types由来のわかりにくいエラーメッセージとの間に立つ**唯一の防波堤**になる。
- **繰り返し発生した構造的なバグパターン**: 「条件式などから型を絞り込んで再構築する」ヘルパーが、型全体をコピーせず**特定の1フィールドだけを個別にコピーして再構築する**実装になっていると、そのフィールドを主に使わない複合型全てに対して静かに壊れる。このバグはポインタで一度見つかって直した後、数ステップ後にリスト・mapでも独立に再発した——1箇所直したときに「他の複合型でも同じ形のバグが無いか」を横展開しなかったのが原因。
- §1で触れた「読み取り・複合書き込みはコピー往復で回避できるが、アドレス取得だけは回避できない」という非対称性は、自動的なブリッジ処理(値レシーバー↔ポインタレシーバーの暗黙変換など)にも波及する。既存のブリッジ処理が「コピーしてから操作する」という単純な実装になっている箇所は、アドレス取得可能な式の集合を広げるたびに再点検すること。
- **意味検査側の「一度導入した制約が、後日の別の改修で前提から崩れる」ケースへの対処**: クロージャーのネスト検出用カウンタは、amivmが`CLOS`のネストを禁止していた間は「>0なら拒否」という単純な用途で正しく機能していたが、amivmが制約を緩めた後は**カウンタごと丸ごと削除するだけで済んだ**——スコープの親子連鎖・結果型のsave/restore・ループ深度のリセットは、いずれも「明示的な深さ追跡」ではなく「再帰呼び出しのたびに独立して動く」設計だったため。**意味検査の制約を実装する際、本当にその制約の理由だけを表す最小の状態に留めておくと、後で制約自体が無くなったときに撤去が綺麗に済む。**
- **今回の制御フロー命令の全面刷新でも、この独立性が真価を発揮した**: `internal/sema`は「if/elif/else・while・for・switch・break・continue」という言語レベルの意味論だけを検証しており、それがamivm側でどんな命令列に落ちるかを一切知らない。そのためamivmの制御フロー命令が単一行`IF`からブロック形`IF`/`LOOP`へ全面刷新されても、semaパッケージは1行も変更する必要が無かった——codegenパッケージだけを書き換えれば済んだ。

## 9. AMIVM本体への機能要求が正当化される基準

1. **コピー往復で完全に回避できる**(`PGET`/`PSET`/`FSET`の「変数のみ」制約): amivm側の変更は任意。あれば便利だが、無くても言語機能として困らない。
2. **専用のGoランタイムパッケージを新設しないと実現不能**(map走査命令の欠如): 強い機能要求の根拠になる。
3. **原理的に回避不能**(`ADDR`のフィールド/添字拡張——エイリアシングはコピーでは絶対に代替できない): 必須の機能要求。
4. **表現力そのものを制限している**(`CLOS`のネスト禁止——カリー化のような言語機能自体を丸ごと表現不能にする): これも3.と並ぶ強い機能要求の根拠。コピー往復や専用ランタイムといった迂回策が原理的に存在しない。

次の言語でamivmへの機能要求を検討する際も、この4段階のどこに位置するかで優先度を判断するとよい。なお今回の制御フロー命令の刷新(§7)はCascade側からの「機能要求」ではなく、amivm自身が複数言語実装の経験から自発的に行った基盤刷新だった——**フロントエンド言語側は要求する立場だけでなく、amivm側の自発的な刷新を受け止める立場にもなりうる**ことを踏まえ、両方への備え(要求の優先度判断基準と、破壊的変更への追従コストを下げる設計)を持っておくとよい。

## 10. 開発プロセス上の教訓の再確認

- 「機能単位の縦切り+各ステップでamivm→go buildの実地検証必須」は変わらず最重要の教訓。「ロジック上正しそうに見えても実地検証なしでは信用しない」で複数のバグを発見した(§1・§4のcallname制約、§7のラベル未参照バグなど)。
- **単体テストが通り、amivmへ一度通した実績がある機能でも、後から入った別の変更(オペランドカテゴリの拡張、制御フロー命令の全面刷新など)が、既に「完成」扱いだった別の機能を静かに壊しうる。** 1つの機能を実装するたびに新しいサンプルだけを確認するのではなく、**既存の全サンプルプログラムを定期的に丸ごと再実行する**ことで、機能横断的な退行を捕まえられた。
- **大規模な後方互換なし変更が来たときは、先に同じ改修に追随した別の言語実装があれば、それを積極的にテンプレートとして参照する価値が大きい。** 今回の制御フロー命令刷新では、Seedが一足先に追随しておりその移行知見(§7で触れたELIFネスト構築・ラベル未参照バグへの対策)を記録していたため、Cascade側はゼロから設計判断をやり直す必要が無く、移行の労力を大幅に削減できた——「知見のある移行」と「ゼロから設計判断する移行」の労力差は体感できるほど大きい。
- **新旧のツールバイナリを並行して手元に残しておくと、大規模な破壊的変更の検証が楽になる。** 今回は新旧amivmバイナリを両方インストールした状態で作業し、新バイナリで全既存サンプルの再実行・多数の使い捨てエッジケースサンプルの実行を行った。ロールバックの選択肢が常に手元にあるという安心感もあり、大胆に書き換えを進められた。
- 意味的に等価な表記の切り替えでも(bare`&N`→完全修飾`&L-N`移行など)、既存の回帰テストの文字列アサーションは軒並み更新が要る。生成IRの厳密な内容を検証するテストは、こうした「同じ意味だが表記が変わる」改修のたびに機械的な更新コストを払うことになる——`strings.Contains`ベースの部分一致テストにしておくことで、無関係な一時変数番号のズレなどまで巻き込まれずに済む。
- **amivm本体からの変更が全て「フロントエンド側もコードを書く」ことを要求するとは限らない。** ジェネリクス対応+`METHVAL`/`FUNCVAL`/`FUNCM`/`INTYPE`/`GETYPE`追加(§12参照)は、既存命令への**オプションな追加**(使わなければ従来の非ジェネリクス構文がそのまま通る後方互換設計)だったため、Cascade側は1行もコードを変更せずに済んだ。ただし「たぶん大丈夫」で済ませず、既存の全サンプルプログラムを新バージョンのamivmに通して実地確認するという手順(前掲の教訓)はこの手の「無変更のはず」の変更でも省略しないこと——実際に無変更で済むかどうかは、確認して初めて確定する。
- **外部ツール(amivm)のリポジトリがPublic化されたら、CIへの組み込みを後回しにしない。** Privateだった間は「実際にamivmへ通してgo buildまで確認する」という本プロジェクトが繰り返し強調してきた検証が手元でしか実行できず、CIには`go test`(フロントエンド自身のsema/codegenのユニットテストのみ)しか乗せられなかった。Public化はGitHub Actions上で認証情報無しに`go install <amivmのモジュールパス>/cmd/<amivm相当>@latest`を実行できるようになる契機であり、これにより「サンプル一式をamivm→go build→実行まで通す」統合テストをpush/PRのたびに自動実行できるようになる。この移行機会を逃さず、Public化に気づいた時点でCIワークフローを追加する価値がある(手元でしか回らない検証は、忘れられたり省略されたりするリスクが常にある)。

## 11. 実務メモ(細かいもの)

- 独自のGoランタイムパッケージ(map走査ヘルパー等、amivmが後から追加した命令で不要になれば削除してよい一時しのぎとして)は、`go:embed`で自分自身のソースを埋め込み、コンパイラのCLIがビルドのたびにスクラッチ用の使い捨てGoモジュールディレクトリへコピーして「同一モジュール内のサブパッケージ」として解決させると、ネットワークアクセスも`replace`ディレクティブも不要でセルフコンテインドになる。**この方式は「amivm本体に対応命令が無い間だけの一時的な迂回」と割り切っておくと、後日ネイティブ命令が追加されたときに気持ちよく削除できる**(実際にmap走査で1回導入し、`MPKEYS`追加後に完全撤去した)。
- 「本来使いたいGo識別子がオペランドカテゴリに含まれない」場合(例: `?os.Stderr`を`value`として渡したかったが当時のカテゴリに`?xxx`が無かった)、無理に含めようとする前に、別の既存カテゴリで表現できる代替経路が無いか検討する価値がある(該当ケースでは標準出力への出力で代替し問題を回避できた)。
- amivmの`-i name=path`は未使用でも自動的に消えるため、依存の有無に関わらず同じマッピング一式を全IRファイルに使い回してよい。ただし依存が完全に無くなったら(ランタイムパッケージ自体を削除したら)`-i`引数自体も一緒に消しておくこと——動くには動くが、もう存在しないパッケージへの参照を引数として渡し続けるのは紛らわしい。
- **知見メモ(このファイルのようなもの)は、それを書いている言語自身のリポジトリに置き、外部ツールの参照用ローカルクローン配下には置かないこと。** 外部クローンは更新のたびに丸ごと`git clone`し直されることがあり、untrackedなファイルはその過程で跡形もなく消える(このファイル自体が過去に2度これで消えた)。各言語のリポジトリで各言語自身の知見を管理する、という方針に統一しておけば、この種の消失リスクを根本的に避けられる。

## 12. 「フロントエンド側の対応が不要」というamivm変更の見分け方

amivm本体はここまで大きく分けて2種類の変更を行ってきた。**(a) 既存の構文を壊す全面刷新**(単一行`IF`/goto方式からブロック形`IF`/`LOOP`への移行——§7)と、**(b) 既存の構文はそのままに、使わなければ影響が無いオプション追加**(ジェネリクス対応+`METHVAL`/`FUNCVAL`/`FUNCM`/`ENDFUNCM`/`INTYPE`/`METHOD`/`ENDINTYPE`/`GETYPE`の追加)である。この2つは対応コストが全く異なるため、見分け方を持っておく価値がある。

- **見分け方**: 変更後の命令仕様(`amivm_spec.md`)で、自分の言語が実際に生成しているIRの形——命令名・オペランドの個数と順序・区切り文字(`:`の数)——が**そのまま今までどおり妥当であり続けるか**を確認する。(a)は既存の生成パターン自体が仕様から削除される(単一行`IF boolean1 label`という形自体が仕様から消えた)ため、生成コード側の書き換えが避けられない。(b)は、新しい構文が既存命令に対して**丸ごと省略可能な追加区画**(`FUNC`の`<<typename constraint ... :>>`のような)として足されているだけで、その区画を一度も書かなければ以前と全く同じIRがそのまま妥当であり続ける。
- **(b)タイプの変更に対する正しい振る舞いは「何もしない」ではなく「何もしなくてよいことを確認する」。** 具体的には、既存の全サンプルプログラムを新バージョンのamivmに通して`amivm`→`go build`→実行まで再確認し(§10で繰り返し強調している手順)、加えて新命令それぞれについて「対応するフロントエンド言語機能が存在するか」を`cascade_spec.md`(自分の言語仕様)と照合する。存在しなければ「対応するCascade機能なし・対象外」として明示的に記録し(判断根拠付きで)、存在すれば実装するかどうかを別途検討する——**存在するのに記録もせず放置すると、次に仕様を読む人が「実装漏れなのか意図的な見送りなのか」を判別できなくなる。**
- 実際に(b)の一例として、`FUNCM`(Goネイティブのレシーバー付きメソッド)はCascadeの中核機能(レシーバー付き関数)に明確に対応する命令でありながら、既存の代替実装(レシーバーを第一引数とする普通の`FUNC`)がその新命令と機能的に同等であるため、切り替えないという判断を下した。**「対応する言語機能がある」ことは「その命令を使うべき」ことを自動的には意味しない**——既存の実装が新命令と同じことを既に達成できているなら、切り替えの利益(コード量削減・将来の拡張性など)とコスト(広範囲な書き換え・回帰リスク)を比較して判断する必要がある。
- **(b)タイプの中でも、「IR側のトークン形状の拡張」(命令そのものではなく、リテラルの書き方が増える類の変更)は判断軸がもう1つ増える。** amivmが整数リテラルへ16進/8進/2進の基数プレフィックス・桁区切り`_`を追加した際、Cascade側のcodegenは影響を受けなかった(`ast.IntLit.Value`を`int64`で保持し、IRへは常に`strconv.FormatInt(v, 10)`で10進として出力する実装だったため、ソース側がどの基数で書かれていたかはcodegenに到達する前に消えている)。**この「フロントエンドの内部表現が最終的に単一の正規化された値に収束する」設計は、IR側のリテラル表記が後から豊かになっても、既存のcodegenを一切変えずに素通りできることの裏返しでもある。** そのため「フロントエンド言語自身の構文をIR側の新しい表記に合わせて拡張するかどうか」は、amivm側の変更から独立した**言語デザインの選択**として扱ってよい(codegenへの影響がゼロなので、やらない場合のコストもゼロ)。Cascadeでは値がint64に正規化された後の話なので、拡張するコストも同様に低かった(lexerとパース時の基数判定だけで完結し、AST・sema・codegenは無変更)——ただし**旧式の「先頭`0`は8進」という解釈だけは意図的に踏襲しなかった**: Go標準の`strconv.ParseInt(lit, 0, 64)`(base 0の自動判定)は`0755`を8進として再解釈してしまうため使わず、明示的な`0x`/`0o`/`0b`プレフィックスの有無を自前で判定してから基数を選ぶヘルパーを書いた。**「Go標準ライブラリの一番手軽な関数」が必ずしも対象言語の既存動作を保つとは限らない**——正規化前の生の文字列を扱う段階では、既存の後方互換性を壊さないかを個別に検討する必要がある。
